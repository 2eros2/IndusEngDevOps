const urlServidor = "http://localhost:4000"

const urlResourcePartidos = urlServidor + "/api/partidos";
const urlResourceEquipos = urlServidor + "/api/equipos";
const urlResourceEstadios= urlServidor + "/api/estadios";

export const config = {
    urlServidor,
    urlResourcePartidos,
    urlResourceEquipos,
    urlResourceEstadios,
}
